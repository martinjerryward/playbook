---
- hosts: localhost
  become: yes

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install i3 window manager
      apt:
        name: i3
        state: present
